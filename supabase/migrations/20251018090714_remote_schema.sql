revoke delete on table "public"."friendships" from "anon";

revoke insert on table "public"."friendships" from "anon";

revoke references on table "public"."friendships" from "anon";

revoke select on table "public"."friendships" from "anon";

revoke trigger on table "public"."friendships" from "anon";

revoke truncate on table "public"."friendships" from "anon";

revoke update on table "public"."friendships" from "anon";

revoke delete on table "public"."friendships" from "authenticated";

revoke insert on table "public"."friendships" from "authenticated";

revoke references on table "public"."friendships" from "authenticated";

revoke select on table "public"."friendships" from "authenticated";

revoke trigger on table "public"."friendships" from "authenticated";

revoke truncate on table "public"."friendships" from "authenticated";

revoke update on table "public"."friendships" from "authenticated";

revoke delete on table "public"."friendships" from "service_role";

revoke insert on table "public"."friendships" from "service_role";

revoke references on table "public"."friendships" from "service_role";

revoke select on table "public"."friendships" from "service_role";

revoke trigger on table "public"."friendships" from "service_role";

revoke truncate on table "public"."friendships" from "service_role";

revoke update on table "public"."friendships" from "service_role";

revoke delete on table "public"."group_members" from "anon";

revoke insert on table "public"."group_members" from "anon";

revoke references on table "public"."group_members" from "anon";

revoke select on table "public"."group_members" from "anon";

revoke trigger on table "public"."group_members" from "anon";

revoke truncate on table "public"."group_members" from "anon";

revoke update on table "public"."group_members" from "anon";

revoke delete on table "public"."group_members" from "authenticated";

revoke insert on table "public"."group_members" from "authenticated";

revoke references on table "public"."group_members" from "authenticated";

revoke select on table "public"."group_members" from "authenticated";

revoke trigger on table "public"."group_members" from "authenticated";

revoke truncate on table "public"."group_members" from "authenticated";

revoke update on table "public"."group_members" from "authenticated";

revoke delete on table "public"."group_members" from "service_role";

revoke insert on table "public"."group_members" from "service_role";

revoke references on table "public"."group_members" from "service_role";

revoke select on table "public"."group_members" from "service_role";

revoke trigger on table "public"."group_members" from "service_role";

revoke truncate on table "public"."group_members" from "service_role";

revoke update on table "public"."group_members" from "service_role";

revoke delete on table "public"."groups" from "anon";

revoke insert on table "public"."groups" from "anon";

revoke references on table "public"."groups" from "anon";

revoke select on table "public"."groups" from "anon";

revoke trigger on table "public"."groups" from "anon";

revoke truncate on table "public"."groups" from "anon";

revoke update on table "public"."groups" from "anon";

revoke delete on table "public"."groups" from "authenticated";

revoke insert on table "public"."groups" from "authenticated";

revoke references on table "public"."groups" from "authenticated";

revoke select on table "public"."groups" from "authenticated";

revoke trigger on table "public"."groups" from "authenticated";

revoke truncate on table "public"."groups" from "authenticated";

revoke update on table "public"."groups" from "authenticated";

revoke delete on table "public"."groups" from "service_role";

revoke insert on table "public"."groups" from "service_role";

revoke references on table "public"."groups" from "service_role";

revoke select on table "public"."groups" from "service_role";

revoke trigger on table "public"."groups" from "service_role";

revoke truncate on table "public"."groups" from "service_role";

revoke update on table "public"."groups" from "service_role";

revoke delete on table "public"."settlements" from "anon";

revoke insert on table "public"."settlements" from "anon";

revoke references on table "public"."settlements" from "anon";

revoke select on table "public"."settlements" from "anon";

revoke trigger on table "public"."settlements" from "anon";

revoke truncate on table "public"."settlements" from "anon";

revoke update on table "public"."settlements" from "anon";

revoke delete on table "public"."settlements" from "authenticated";

revoke insert on table "public"."settlements" from "authenticated";

revoke references on table "public"."settlements" from "authenticated";

revoke select on table "public"."settlements" from "authenticated";

revoke trigger on table "public"."settlements" from "authenticated";

revoke truncate on table "public"."settlements" from "authenticated";

revoke update on table "public"."settlements" from "authenticated";

revoke delete on table "public"."settlements" from "service_role";

revoke insert on table "public"."settlements" from "service_role";

revoke references on table "public"."settlements" from "service_role";

revoke select on table "public"."settlements" from "service_role";

revoke trigger on table "public"."settlements" from "service_role";

revoke truncate on table "public"."settlements" from "service_role";

revoke update on table "public"."settlements" from "service_role";

revoke delete on table "public"."transaction_comments" from "anon";

revoke insert on table "public"."transaction_comments" from "anon";

revoke references on table "public"."transaction_comments" from "anon";

revoke select on table "public"."transaction_comments" from "anon";

revoke trigger on table "public"."transaction_comments" from "anon";

revoke truncate on table "public"."transaction_comments" from "anon";

revoke update on table "public"."transaction_comments" from "anon";

revoke delete on table "public"."transaction_comments" from "authenticated";

revoke insert on table "public"."transaction_comments" from "authenticated";

revoke references on table "public"."transaction_comments" from "authenticated";

revoke select on table "public"."transaction_comments" from "authenticated";

revoke trigger on table "public"."transaction_comments" from "authenticated";

revoke truncate on table "public"."transaction_comments" from "authenticated";

revoke update on table "public"."transaction_comments" from "authenticated";

revoke delete on table "public"."transaction_comments" from "service_role";

revoke insert on table "public"."transaction_comments" from "service_role";

revoke references on table "public"."transaction_comments" from "service_role";

revoke select on table "public"."transaction_comments" from "service_role";

revoke trigger on table "public"."transaction_comments" from "service_role";

revoke truncate on table "public"."transaction_comments" from "service_role";

revoke update on table "public"."transaction_comments" from "service_role";

revoke delete on table "public"."transaction_splits" from "anon";

revoke insert on table "public"."transaction_splits" from "anon";

revoke references on table "public"."transaction_splits" from "anon";

revoke select on table "public"."transaction_splits" from "anon";

revoke trigger on table "public"."transaction_splits" from "anon";

revoke truncate on table "public"."transaction_splits" from "anon";

revoke update on table "public"."transaction_splits" from "anon";

revoke delete on table "public"."transaction_splits" from "authenticated";

revoke insert on table "public"."transaction_splits" from "authenticated";

revoke references on table "public"."transaction_splits" from "authenticated";

revoke select on table "public"."transaction_splits" from "authenticated";

revoke trigger on table "public"."transaction_splits" from "authenticated";

revoke truncate on table "public"."transaction_splits" from "authenticated";

revoke update on table "public"."transaction_splits" from "authenticated";

revoke delete on table "public"."transaction_splits" from "service_role";

revoke insert on table "public"."transaction_splits" from "service_role";

revoke references on table "public"."transaction_splits" from "service_role";

revoke select on table "public"."transaction_splits" from "service_role";

revoke trigger on table "public"."transaction_splits" from "service_role";

revoke truncate on table "public"."transaction_splits" from "service_role";

revoke update on table "public"."transaction_splits" from "service_role";

revoke delete on table "public"."transactions" from "anon";

revoke insert on table "public"."transactions" from "anon";

revoke references on table "public"."transactions" from "anon";

revoke select on table "public"."transactions" from "anon";

revoke trigger on table "public"."transactions" from "anon";

revoke truncate on table "public"."transactions" from "anon";

revoke update on table "public"."transactions" from "anon";

revoke delete on table "public"."transactions" from "authenticated";

revoke insert on table "public"."transactions" from "authenticated";

revoke references on table "public"."transactions" from "authenticated";

revoke select on table "public"."transactions" from "authenticated";

revoke trigger on table "public"."transactions" from "authenticated";

revoke truncate on table "public"."transactions" from "authenticated";

revoke update on table "public"."transactions" from "authenticated";

revoke delete on table "public"."transactions" from "service_role";

revoke insert on table "public"."transactions" from "service_role";

revoke references on table "public"."transactions" from "service_role";

revoke select on table "public"."transactions" from "service_role";

revoke trigger on table "public"."transactions" from "service_role";

revoke truncate on table "public"."transactions" from "service_role";

revoke update on table "public"."transactions" from "service_role";

revoke delete on table "public"."user_profiles" from "anon";

revoke insert on table "public"."user_profiles" from "anon";

revoke references on table "public"."user_profiles" from "anon";

revoke select on table "public"."user_profiles" from "anon";

revoke trigger on table "public"."user_profiles" from "anon";

revoke truncate on table "public"."user_profiles" from "anon";

revoke update on table "public"."user_profiles" from "anon";

revoke delete on table "public"."user_profiles" from "authenticated";

revoke insert on table "public"."user_profiles" from "authenticated";

revoke references on table "public"."user_profiles" from "authenticated";

revoke select on table "public"."user_profiles" from "authenticated";

revoke trigger on table "public"."user_profiles" from "authenticated";

revoke truncate on table "public"."user_profiles" from "authenticated";

revoke update on table "public"."user_profiles" from "authenticated";

revoke delete on table "public"."user_profiles" from "service_role";

revoke insert on table "public"."user_profiles" from "service_role";

revoke references on table "public"."user_profiles" from "service_role";

revoke select on table "public"."user_profiles" from "service_role";

revoke trigger on table "public"."user_profiles" from "service_role";

revoke truncate on table "public"."user_profiles" from "service_role";

revoke update on table "public"."user_profiles" from "service_role";

drop function if exists "public"."get_auth_user_by_email_or_phone"(p_email text, p_phone text);

set check_function_bodies = off;

CREATE OR REPLACE FUNCTION public.add_member_to_group(p_group_id uuid, p_inviter uuid, p_friend uuid, p_meta jsonb, p_invite_token text, p_invite_sent_at timestamp with time zone, p_invite_expires_at timestamp with time zone, p_invite_channel friendship_source_enum)
 RETURNS jsonb
 LANGUAGE plpgsql
AS $function$
declare
  v_inviter_member record;
  v_friend record;
  v_group_member record;
begin
  -- 1) Verify inviter is a member of the group
  select id, role into v_inviter_member
  from public.group_members
  where group_id = p_group_id
    and user_id = p_inviter
  limit 1;

  if not found then
    -- fail fast; edge function should catch this and return 403
    raise exception 'inviter_not_member';
  end if;

  -- Optional: if you want to restrict to admin/owner only, add:
  -- if v_inviter_member.role not in ('admin','owner') then
  --   raise exception 'inviter_not_authorized';
  -- end if;

  -- 2) friendships: insert only if a row for (requester_id, friend_id) does not exist.
  select * into v_friend
  from public.friendships
  where requester_id = p_inviter
    and friend_id = p_friend
  limit 1;

  if not found then
    insert into public.friendships(
      requester_id,
      friend_id,
      source,
      status,
      meta,
      created_at
    ) values (
      p_inviter,
      p_friend,
      coalesce(p_invite_channel, 'whatsapp'),
      'pending',
      coalesce(p_meta, '{}'::jsonb),
      now()
    );
  end if;

  -- 3) group_members: if exists -> UPDATE status='active', invited_by=inviter, removed_by=NULL
  select * into v_group_member
  from public.group_members
  where group_id = p_group_id
    and user_id = p_friend
  limit 1;

  if found then
    update public.group_members
    set status = 'active',
        invited_by = p_inviter,
        removed_by = null,
        left_at = null
    where id = v_group_member.id;
  else
    insert into public.group_members(
      group_id,
      user_id,
      invited_by,
      role,
      status,
      joined_at
    ) values (
      p_group_id,
      p_friend,
      p_inviter,
      'member',
      'active',
      now()
    );
  end if;

  -- Optionally persist an invites log if you have an 'invites' table.
  -- Example (uncomment and adapt if you have this table):
  -- insert into public.invites (group_id, friend_id, inviter_id, invite_token, invite_sent_at, invite_expires_at, channel, meta, created_at)
  -- values (p_group_id, p_friend, p_inviter, p_invite_token, p_invite_sent_at, p_invite_expires_at, p_invite_channel, p_meta, now());

  return jsonb_build_object(
    'ok', true,
    'friend_profile_id', p_friend,
    'invite_token', p_invite_token,
    'message', 'rpc_add_invite_to_group: success'
  );
exception
  when others then
    -- Normalize the error message so edge function can interpret
    return jsonb_build_object(
      'ok', false,
      'error', sqlstate || ': ' || coalesce(nullif(sqlerrm, ''), 'unknown error')
    );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.create_group(name text, avatar_url text DEFAULT NULL::text)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public', 'pg_temp'
AS $function$
DECLARE
  _uid uuid := auth.uid();
  _group_record public.groups%ROWTYPE;
BEGIN
  -- auth check
  IF _uid IS NULL THEN
    RETURN jsonb_build_object('ok', false, 'error', 'Unauthorized');
  END IF;

  -- inviter profile must exist
  IF NOT EXISTS (SELECT 1 FROM public.user_profiles WHERE id = _uid) THEN
    RETURN jsonb_build_object('ok', false, 'error', 'Inviter profile not found');
  END IF;

  -- validate name
  IF name IS NULL OR length(trim(name)) = 0 THEN
    RETURN jsonb_build_object('ok', false, 'error', 'Group name is required');
  END IF;

  -- Insert group (handle unique constraint and other DB errors gracefully)
  BEGIN
    INSERT INTO public.groups (name, avatar_url, created_by)
    VALUES (trim(name), avatar_url, _uid)
    RETURNING * INTO _group_record;
  EXCEPTION
    WHEN unique_violation THEN
      RETURN jsonb_build_object('ok', false, 'error', 'Group with this name already exists');
    WHEN others THEN
      RETURN jsonb_build_object('ok', false, 'error', sqlerrm);
  END;

  -- Attempt to insert owner member; non-fatal — log a notice if it fails
  BEGIN
    INSERT INTO public.group_members (group_id, user_id, role, invited_by)
    VALUES (_group_record.id, _uid, 'owner', _uid);
  EXCEPTION
    WHEN others THEN
      -- NOTE: NOTICE is visible in DB logs but not returned to client; keeps RPC response clean
      RAISE NOTICE 'create_group_rpc: group_members insert warning: %', sqlerrm;
      -- continue and return success for group creation
  END;

  -- Success
  RETURN jsonb_build_object(
    'ok', true,
    'data', row_to_json(_group_record),
    'message', 'Group created successfully'
  );

EXCEPTION
  WHEN others THEN
    -- generic fallback; avoid leaking internal details but return something useful
    RETURN jsonb_build_object('ok', false, 'error', sqlerrm);
END;
$function$
;

CREATE OR REPLACE FUNCTION public.create_group_invite(_inviter_profile_id uuid, _group_id uuid, _placeholder_profile_id uuid, _phone text, _email text, _contact_name text, _invite_channel text, _invite_flow text DEFAULT 'share'::text, _inviter_source text DEFAULT NULL::text, _invite_expires_days integer DEFAULT 7, _app_invite_base_url text DEFAULT 'evenup://invite'::text)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  v_phone_hash text := null;
  v_email_hash text := null;
  v_invite_token text := null;
  v_invite_sent_at timestamptz := now();
  v_invite_expires_at timestamptz := now() + (_invite_expires_days || ' days')::interval;
  v_meta jsonb;
  v_friend_id uuid := _placeholder_profile_id;
begin
  -- compute hashes if present
  if _phone is not null then
    v_phone_hash := encode(digest(_phone, 'sha256'), 'hex');
  end if;
  if _email is not null then
    v_email_hash := encode(digest(_email, 'sha256'), 'hex');
  end if;

  -- If no placeholder_profile_id provided, try to find existing profile by hash
  if v_friend_id is null then
    if v_phone_hash is not null then
      select id into v_friend_id from user_profiles where phone_hash = v_phone_hash limit 1;
    end if;

    if v_friend_id is null and v_email_hash is not null then
      select id into v_friend_id from user_profiles where email_hash = v_email_hash limit 1;
    end if;

    -- If still null, create a placeholder profile row
    if v_friend_id is null then
      insert into user_profiles (
        nickname, email, phone, avatar_url, invited_by, phone_hash, email_hash,
        status, metadata
      ) values (
        _contact_name, _email, _phone, null, _inviter_profile_id, v_phone_hash, v_email_hash,
        'invited',
        jsonb_build_object('placeholder', true, 'contact_name', _contact_name, 'canonical_phone', _phone)
      ) returning id into v_friend_id;
    end if;
  end if;

  -- generate invite token via existing RPC
  -- attempt to handle multiple possible shapes returned by generate_invite_token
  begin
    -- try as text
    select (generate_invite_token())::text into v_invite_token;
  exception when others then
    begin
      -- try as record with invite_token field
      select (generate_invite_token()).invite_token::text into v_invite_token;
    exception when others then
      v_invite_token := null;
    end;
  end;

  if v_invite_token is null then
    raise exception 'Failed to generate invite token';
  end if;

  v_meta := jsonb_build_object(
    'invite_token', v_invite_token,
    'invite_sent_at', to_char(v_invite_sent_at, 'YYYY-MM-DD"T"HH24:MI:SS"Z"'),
    'invite_expires_at', to_char(v_invite_expires_at, 'YYYY-MM-DD"T"HH24:MI:SS"Z"'),
    'invite_flow', _invite_flow,
    'invite_channel', _invite_channel,
    'invite_status', 'pending',
    'invite_from', _inviter_profile_id,
    'share_confirmed', false,
    'invite_attempts', 1,
    'contact_name', _contact_name,
    'normalized_phone', _phone,
    'normalized_email', _email
  );

  -- upsert friendships (use explicit column names to avoid ambiguity)
  insert into friendships (
    requester_id, friend_id, source, status, meta
  ) values (
    _inviter_profile_id, v_friend_id, coalesce(_inviter_source, (case when _phone is not null then 'phone' when _email is not null then 'email' end)), 'pending', v_meta
  )
  on conflict (requester_id, friend_id)
  do update set
    meta = excluded.meta,
    status = excluded.status,
    source = excluded.source
  ;

  -- upsert group_members
  insert into group_members (group_id, user_id, invited_by, role)
  values (_group_id, v_friend_id, _inviter_profile_id, 'member')
  on conflict (group_id, user_id) do update set
    invited_by = coalesce(group_members.invited_by, excluded.invited_by)
  ;

  return jsonb_build_object(
    'ok', true,
    'invite_token', v_invite_token,
    'friend_profile_id', v_friend_id,
    'invite_sent_at', to_char(v_invite_sent_at, 'YYYY-MM-DD"T"HH24:MI:SS"Z"'),
    'invite_expires_at', to_char(v_invite_expires_at, 'YYYY-MM-DD"T"HH24:MI:SS"Z"'),
    'invite_link', _app_invite_base_url || '?token=' || v_invite_token || '&friend_id=' || v_friend_id
  );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.create_group_transaction_with_splits(payload jsonb)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  p jsonb := payload; -- canonical payload we will use
  tx_id uuid;
  t_title text;
  t_amount numeric;
  t_currency text;
  t_paid_by uuid;
  t_group_id uuid;
  t_created_by uuid;
  t_receipt_url text;
  t_status text;
  t_metadata jsonb;
  splits jsonb;
  s jsonb;
  s_user uuid;
  s_amount numeric;
  s_share_type text;
  s_share_meta jsonb;
  sum_splits numeric := 0;
  created_at_ts timestamp with time zone;
  _inner_text text;
begin
  /*
    Normalise payload shapes:
      - payload is already object => use as-is
      - payload = { "payload": <object> } => use inner object
      - payload = { "payload": "<stringified json>" } => parse inner string into jsonb
  */
  if p ? 'payload' then
    -- try to extract inner as jsonb; if inner is a JSON string (stringified JSON),
    -- casting -> try (p->>'payload')::jsonb
    begin
      -- if inner is a json value (object/array), this will succeed
      p := (p->'payload')::jsonb;
    exception when others then
      -- fallback: if inner is a string containing JSON, parse that string
      _inner_text := p->>'payload';
      if _inner_text is not null and length(btrim(_inner_text)) > 0 then
        p := _inner_text::jsonb;
      else
        -- inner payload empty -> keep original p (will fail validation later)
        p := '{}'::jsonb;
      end if;
    end;
  end if;

  -- extract fields from canonical payload 'p'
  t_title := nullif(btrim(p ->> 'title'), ''); -- treat empty string as null
  -- amount might be a number or a string, coerce safely
  begin
    t_amount := (p ->> 'amount')::numeric;
  exception when others then
    t_amount := null;
  end;
  t_currency := coalesce(p ->> 'currency', 'INR');
  begin
    t_paid_by := (p ->> 'paidBy')::uuid;
  exception when others then
    t_paid_by := null;
  end;
  begin
    t_group_id := (p ->> 'groupId')::uuid;
  exception when others then
    t_group_id := null;
  end;
  begin
    t_created_by := (p ->> 'createdBy')::uuid;
  exception when others then
    t_created_by := null;
  end;
  t_receipt_url := p ->> 'receiptUrl';
  t_status := coalesce(p ->> 'status', 'active');
  t_metadata := coalesce(p -> 'metadata', '{}'::jsonb);
  splits := p -> 'splits';

  -- Basic validation
  if t_title is null then
    return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'title is required');
  end if;

  if t_amount is null or t_amount <= 0 then
    return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'amount must be > 0');
  end if;

  if t_paid_by is null then
    return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'paidBy is required');
  end if;

  if splits is null or jsonb_typeof(splits) is distinct from 'array' or jsonb_array_length(splits) = 0 then
    return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'splits are required');
  end if;

  -- validate split objects and sum
  for s in select * from jsonb_array_elements(splits)
  loop
    begin
      s_user := (s ->> 'userId')::uuid;
    exception when others then
      s_user := null;
    end;
    begin
      s_amount := (s ->> 'amount')::numeric;
    exception when others then
      s_amount := null;
    end;
    s_share_type := coalesce(s ->> 'shareType', 'exact');
    s_share_meta := coalesce(s -> 'shareMeta', '{}'::jsonb);

    if s_user is null then
      return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'each split must have userId');
    end if;
    if s_amount is null or s_amount < 0 then
      return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'each split must have non-negative amount');
    end if;
    sum_splits := sum_splits + s_amount;
  end loop;

  -- enforce that sum of splits equals amount (tweak tolerance if you need)
  if abs(sum_splits - t_amount) > 0.01 then
    return jsonb_build_object(
      'ok', false,
      'error', 'validation_error',
      'message', format('sum of splits (%.2f) does not equal transaction amount (%.2f)', sum_splits, t_amount)
    );
  end if;

  -- create transaction
  insert into public.transactions(
    title, amount, currency, paid_by, group_id, status, receipt_url, created_by, created_at, metadata
  )
  values (
    t_title, t_amount, t_currency, t_paid_by, t_group_id, t_status, t_receipt_url, t_created_by, now(), coalesce(t_metadata, '{}'::jsonb)
  )
  returning id, created_at into tx_id, created_at_ts;

  -- insert splits
  for s in select * from jsonb_array_elements(splits)
  loop
    s_user := (s ->> 'userId')::uuid;
    s_amount := (s ->> 'amount')::numeric;
    s_share_type := coalesce(s ->> 'shareType', 'exact');
    s_share_meta := coalesce(s -> 'shareMeta', '{}'::jsonb);

    insert into public.transaction_splits(transaction_id, user_id, amount, share_type, share_meta, created_at)
    values (tx_id, s_user, s_amount, s_share_type, s_share_meta, now());
  end loop;

  -- success -> return standardized wrapper with data
  return jsonb_build_object(
    'ok', true,
    'data', jsonb_build_object('transactionId', tx_id, 'createdAt', to_char(created_at_ts, 'YYYY-MM-DD"T"HH24:MI:SSOF'))
  );
exception
  when others then
    -- Return standardized error response (do NOT re-raise)
    return jsonb_build_object(
      'ok', false,
      'error', 'server_error',
      'message', coalesce(sqlerrm, 'unknown error')
    );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.create_profile_for_auth_user(p_uid uuid, p_email text, p_phone text, p_nickname text DEFAULT NULL::text, p_metadata jsonb DEFAULT NULL::jsonb, p_theme theme_enum DEFAULT 'light'::theme_enum, p_language text DEFAULT 'en'::text, p_currency text DEFAULT 'INR'::text, p_invited_by uuid DEFAULT NULL::uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  v_email_hash text := null;
  v_phone_hash text := null;
begin
  -- normalize inputs (lowercase email, trim phone)
  if p_email is not null then
    v_email_hash := encode(digest(lower(trim(p_email))::bytea, 'sha256'), 'hex');
  end if;

  if p_phone is not null then
    v_phone_hash := encode(digest(trim(p_phone)::bytea, 'sha256'), 'hex');
  end if;

  -- insert profile (adjust columns to match your schema)
  insert into public.user_profiles (
    id,
    email,
    phone,
    avatar_url,
    currency,
    language,
    theme,
    nickname,
    status,
    invited_by,
    last_active_at,
    metadata,
    phone_hash,
    email_hash
  ) values (
    p_uid,
    p_email,
    p_phone,
    null,
    p_currency,
    p_language,
    p_theme,
    p_nickname,
    'active',
    p_invited_by,
    null,
    p_metadata,
    v_phone_hash,
    v_email_hash
  );

  return jsonb_build_object('ok', true, 'message', 'profile_created', 'user_id', p_uid);

exception
  when unique_violation then
    return jsonb_build_object('ok', false, 'error', 'db_conflict', 'message', sqlerrm);
  when others then
    return jsonb_build_object('ok', false, 'error', 'db_error', 'message', sqlerrm);
end;
$function$
;

CREATE OR REPLACE FUNCTION public.create_transaction_comment(payload jsonb)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  tx_id uuid := (payload ->> 'transaction_id')::uuid;
  created_by uuid := (payload ->> 'created_by')::uuid;
  body text := payload ->> 'body';
  new_id uuid;
  created_ts timestamptz;
begin
  if tx_id is null or created_by is null or body is null or trim(body) = '' then
    return jsonb_build_object('ok', false, 'error', 'validation_error', 'message', 'Missing required fields');
  end if;

  insert into public.transaction_comments (transaction_id, created_by, body, created_at)
  values (tx_id, created_by, body, now())
  returning id, created_at into new_id, created_ts;

  return jsonb_build_object(
    'ok', true,
    'data', jsonb_build_object(
      'id', new_id,
      'transaction_id', tx_id,
      'created_by', created_by,
      'body', body,
      'created_at', to_char(created_ts, 'YYYY-MM-DD"T"HH24:MI:SSOF')
    )
  );
exception when others then
  return jsonb_build_object('ok', false, 'error', 'server_error', 'message', sqlerrm);
end;
$function$
;

CREATE OR REPLACE FUNCTION public.delete_group(p_group_id uuid, p_user_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  rows_affected integer;
begin
  update public.groups
  set 
    deleted_by = p_user_id,
    deleted_at = now(),
    status = 'deleted'
  where id = p_group_id;

  get diagnostics rows_affected = row_count;

  if rows_affected = 0 then
    return jsonb_build_object(
      'ok', false,
      'error', 'Group not found',
      'message', format('No group found for id: %s', p_group_id)
    );
  end if;

  return jsonb_build_object(
    'ok', true,
    'message', 'Group deleted successfully',
    'data', jsonb_build_object('group_id', p_group_id)
  );

exception
  when others then
    return jsonb_build_object(
      'ok', false,
      'error', SQLERRM,
      'message', 'Failed to delete group'
    );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.delete_group_member(p_group_id uuid, p_user_id uuid, p_removed_by uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
AS $function$
DECLARE
  updated_row public.group_members%ROWTYPE;
  -- declare variable using the same type as the table's status column
  new_status public.group_members.status%TYPE;
BEGIN
  -- Determine status based on who removed the user
  IF p_user_id = p_removed_by THEN
    new_status := 'left'::public.group_member_status_enum;
  ELSE
    new_status := 'deleted'::public.group_member_status_enum;
  END IF;

  UPDATE public.group_members
  SET
    status = new_status,
    removed_by = p_removed_by,
    left_at = now()
  WHERE
    group_id = p_group_id
    AND user_id = p_user_id
    AND left_at IS NULL
    AND (status IS NULL OR status NOT IN (
      'deleted'::public.group_member_status_enum,
      'left'::public.group_member_status_enum
    ))
  RETURNING * INTO updated_row;

  IF NOT FOUND THEN
    RETURN jsonb_build_object(
      'ok', false,
      'error', 'no_matching_active_member',
      'message', 'No active group_member row found for the given group_id and user_id'
    );
  END IF;

  RETURN jsonb_build_object(
    'ok', true,
    'data', to_jsonb(updated_row)
  );
END;
$function$
;

CREATE OR REPLACE FUNCTION public.generate_invite_token()
 RETURNS text
 LANGUAGE plpgsql
AS $function$
declare
  b bytea := gen_random_bytes(32);
begin
  return encode(b, 'base64');
end;
$function$
;

CREATE OR REPLACE FUNCTION public.get_friends(p_user_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 STABLE
AS $function$
DECLARE
  friends jsonb;
BEGIN
  SELECT COALESCE(
    jsonb_agg(
      jsonb_build_object(
        'friendship_id', f.id,
        'friend_id', CASE WHEN f.requester_id = p_user_id THEN f.friend_id ELSE f.requester_id END::text,
        'nickname', up.nickname,
        'email', up.email,
        'phone', up.phone,
        'status', up.status
      )
    ),
    '[]'::jsonb
  ) INTO friends
  FROM friendships f
  JOIN user_profiles up
    ON up.id = (CASE WHEN f.requester_id = p_user_id THEN f.friend_id ELSE f.requester_id END)::uuid
  WHERE p_user_id IN (f.requester_id, f.friend_id);

  RETURN jsonb_build_object(
    'ok', true,
    'data', friends
  );
EXCEPTION WHEN OTHERS THEN
  RETURN jsonb_build_object(
    'ok', false,
    'error', SQLERRM,
    'message', 'Failed to fetch friends'
  );
END;
$function$
;

CREATE OR REPLACE FUNCTION public.get_group_details(p_group_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 STABLE
AS $function$
DECLARE
  effective_user uuid;
  group_exists boolean;
  member_exists boolean;
  result jsonb;
BEGIN
  -- Determine caller user id (prefer auth.uid() in Supabase)
  BEGIN
    effective_user := auth.uid();
  EXCEPTION WHEN others THEN
    effective_user := NULL;
  END;

  IF effective_user IS NULL THEN
    BEGIN
      effective_user := (current_setting('request.jwt.claims.sub', true))::uuid;
    EXCEPTION WHEN others THEN
      effective_user := NULL;
    END;
  END IF;

  IF effective_user IS NULL THEN
    RETURN jsonb_build_object(
      'ok', false,
      'error', 'no_user_identified',
      'message', 'No user identity available in JWT. Ensure the request includes a valid Supabase access token.'
    );
  END IF;

  -- 1) Check that the group exists and is active
  SELECT EXISTS (
    SELECT 1 FROM public.groups g
    WHERE g.id = p_group_id
      AND g.status = 'active'
  ) INTO group_exists;

  IF NOT group_exists THEN
    RETURN jsonb_build_object(
      'ok', false,
      'error', 'group_not_found_or_inactive',
      'message', 'Group not found or not active.'
    );
  END IF;

  -- 2) Check active membership (only after confirming group exists)
  SELECT EXISTS (
    SELECT 1
    FROM public.group_members gm
    WHERE gm.group_id = p_group_id
      AND gm.user_id = effective_user
      AND gm.left_at IS NULL
      AND (gm.status IS NULL OR gm.status NOT IN ('deleted','left'))
  ) INTO member_exists;

  IF NOT member_exists THEN
    RETURN jsonb_build_object(
      'ok', false,
      'error', 'not_a_group_member',
      'message', 'You are not a member of this group.'
    );
  END IF;

  -- 3) Build and return the group object
  SELECT jsonb_build_object(
    'id', g.id,
    'name', g.name,
    'avatar_url', g.avatar_url,
    'simplified', g.simplified,
    'members',
      COALESCE(
        (
          SELECT jsonb_agg(member_obj)
          FROM (
            SELECT
              jsonb_build_object(
                'id', up.id,
                'name', up.nickname,
                'phone', up.phone,
                'email', up.email,
                'avatar_url', up.avatar_url,
                'currency', up.currency,
                'language', up.language,
                'account_status', up.status,
                'status_in_group', gm.status,
                'role', gm.role,
                'joined_at', gm.joined_at
              ) AS member_obj
            FROM public.group_members gm
            JOIN public.user_profiles up ON up.id = gm.user_id
            WHERE gm.group_id = g.id
              AND gm.left_at IS NULL
            ORDER BY
              (gm.role = 'owner') DESC,
              gm.joined_at NULLS LAST
          ) sub
        ), '[]'::jsonb
      )
  ) INTO result
  FROM public.groups g
  WHERE g.id = p_group_id
    AND g.status = 'active';

  -- Should be found since we checked earlier, but keep the guard
  IF result IS NULL THEN
    RETURN jsonb_build_object(
      'ok', false,
      'error', 'group_not_found_or_inactive',
      'message', 'Group not found or not active.'
    );
  END IF;

  RETURN jsonb_build_object('ok', true, 'data', result);
END;
$function$
;

CREATE OR REPLACE FUNCTION public.get_group_transactions_paginated(p_group_id uuid, p_user_id uuid, p_limit integer DEFAULT 10, p_offset integer DEFAULT 0)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  tx_rows jsonb;
  total_spent numeric := 0;
  you_owe numeric := 0;
  friends_owe numeric := 0;
begin
  /*
    Select the page of transactions using an inner subquery to
    apply ORDER BY + LIMIT/OFFSET deterministically, then aggregate
    that subquery into jsonb.
  */
  select coalesce(jsonb_agg(jsonb_build_object(
    'id', t.id,
    'title', t.title,
    'amount', t.amount,
    'paidBy', t.paid_by,
    'paidByName', coalesce(up.nickname, up.email, up.phone, 'Unknown'),
    'paidByAvatar', up.avatar_url,
    'createdAt', to_char(t.created_at, 'YYYY-MM-DD"T"HH24:MI:SSOF'),
    'hasAttachment', (t.receipt_url is not null)
  )), '[]'::jsonb)
  into tx_rows
  from (
    select *
    from public.transactions tt
    where tt.group_id = p_group_id and tt.status <> 'deleted'
    order by tt.created_at desc
    limit p_limit offset p_offset
  ) t
  left join public.user_profiles up on up.id = t.paid_by;

  -- Totals (unchanged)
  select coalesce(sum(amount), 0) into total_spent
  from public.transactions
  where group_id = p_group_id and status <> 'deleted';

  with splits as (
    select ts.user_id, ts.amount, t.paid_by
    from public.transaction_splits ts
    join public.transactions t on t.id = ts.transaction_id
    where t.group_id = p_group_id and t.status <> 'deleted'
  )
  select
    coalesce(sum(case when s.user_id = p_user_id and s.user_id <> s.paid_by then s.amount end), 0),
    coalesce(sum(case when s.paid_by = p_user_id and s.user_id <> s.paid_by then s.amount end), 0)
  into you_owe, friends_owe
  from splits s;

  return jsonb_build_object(
    'ok', true,
    'data', jsonb_build_object(
      'transactions', tx_rows,
      'summary', jsonb_build_object(
        'totalSpent', total_spent,
        'youOwe', you_owe,
        'friendsOwe', friends_owe
      )
    )
  );
exception when others then
  return jsonb_build_object('ok', false, 'error', 'server_error', 'message', sqlerrm);
end;
$function$
;

CREATE OR REPLACE FUNCTION public.get_or_create_friendship(user_a uuid, user_b uuid, in_source text DEFAULT 'manual'::text, in_status text DEFAULT 'accepted'::text)
 RETURNS uuid
 LANGUAGE plpgsql
AS $function$
declare
  f_id uuid;
begin
  if user_a is null or user_b is null then
    raise exception 'user ids required';
  end if;
  if user_a = user_b then
    raise exception 'cannot create friendship with self';
  end if;

  -- attempt to find existing non-deleted friendship
  select id into f_id
  from public.friendships
  where (
    (requester_id = user_a and friend_id = user_b)
    or (requester_id = user_b and friend_id = user_a)
  ) and coalesce(status,'pending') <> 'deleted'
  limit 1;

  if found then
    return f_id;
  end if;

  -- not found: insert (requester_id = user_a for audit)
  insert into public.friendships
    (requester_id, friend_id, source, status, created_at)
  values
    (user_a, user_b, in_source, in_status, now())
  returning id into f_id;

  return f_id;
end;
$function$
;

CREATE OR REPLACE FUNCTION public.get_transaction_details(p_tx_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
declare
  tx record;
  parts jsonb;
  cmts jsonb;
begin
  select t.*, coalesce(up.nickname, up.email, up.phone, 'Unknown') as payer_name, up.avatar_url as payer_avatar
  into tx
  from public.transactions t
  left join public.user_profiles up on up.id = t.paid_by
  where t.id = p_tx_id;

  if not found then
    return jsonb_build_object('ok', false, 'error', 'not_found', 'message', 'Transaction not found');
  end if;

  select coalesce(jsonb_agg(jsonb_build_object(
    'userId', ts.user_id,
    'amount', ts.amount,
    'shareType', ts.share_type,
    'name', coalesce(up.nickname, up.email, up.phone, 'Unknown'),
    'avatar_url', up.avatar_url
  ) order by ts.created_at), '[]'::jsonb) into parts
  from public.transaction_splits ts
  left join public.user_profiles up on up.id = ts.user_id
  where ts.transaction_id = p_tx_id;

  select coalesce(jsonb_agg(jsonb_build_object(
    'id', c.id,
    'userId', c.created_by,
    'user', coalesce(u.nickname, u.email, u.phone, 'Unknown'),
    'message', c.body,
    'createdAt', to_char(c.created_at, 'YYYY-MM-DD"T"HH24:MI:SSOF')
  ) order by c.created_at desc), '[]'::jsonb) into cmts
  from public.transaction_comments c
  left join public.user_profiles u on u.id = c.created_by
  where c.transaction_id = p_tx_id;

  return jsonb_build_object(
    'ok', true,
    'data', jsonb_build_object(
      'id', tx.id,
      'title', tx.title,
      'amount', tx.amount,
      'currency', tx.currency,
      'paidBy', tx.paid_by,
      'paidByName', tx.payer_name,
      'paidByAvatar', tx.payer_avatar,
      'date', to_char(tx.created_at, 'YYYY-MM-DD'),
      'splitMethod', (tx.metadata ->> 'splitMethod')::text,
      'participants', parts,
      'comments', cmts
    )
  );
exception when others then
  return jsonb_build_object('ok', false, 'error', 'server_error', 'message', sqlerrm);
end;
$function$
;

CREATE OR REPLACE FUNCTION public.get_user_groups(p_user_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 STABLE SECURITY DEFINER
AS $function$
DECLARE
  groups jsonb;
BEGIN
  SELECT COALESCE(jsonb_agg(group_json), '[]'::jsonb)
  INTO groups
  FROM (
    SELECT
      jsonb_build_object(
        'id', g.id,
        'name', g.name,
        'avatar_url', g.avatar_url,
        'created_at', g.created_at,
        'created_by', g.created_by,
        'simplified', g.simplified,
        'updated_at', g.updated_at
      ) AS group_json
    FROM groups g
    WHERE EXISTS (
      SELECT 1
      FROM group_members gm2
      WHERE gm2.group_id = g.id
        AND gm2.user_id = p_user_id
        AND gm2.left_at IS NULL
    )
    AND g.status = 'active'
    ORDER BY g.updated_at DESC
  ) t;

  RETURN jsonb_build_object(
    'ok', true,
    'data', groups
  );
EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object(
      'ok', false,
      'error', SQLERRM,
      'message', 'Failed to fetch user groups'
    );
END;
$function$
;

CREATE OR REPLACE FUNCTION public.get_user_profile(p_user_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
DECLARE
  user_json jsonb;
BEGIN
  SELECT to_jsonb(up) INTO user_json
  FROM public.user_profiles up
  WHERE up.id = p_user_id
  LIMIT 1;

  IF user_json IS NULL THEN
    -- not found: return an application-level error envelope
    RETURN jsonb_build_object(
      'ok', FALSE,
      'error', 'not_found',
      'message', format('No user found for id: %s', p_user_id)
    );
  END IF;

  RETURN jsonb_build_object(
    'ok', TRUE,
    'data', user_json
  );

EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object(
      'ok', FALSE,
      'error', 'internal_error',
      'message', SQLERRM
    );
END;
$function$
;

CREATE OR REPLACE FUNCTION public.get_user_profile_by_id(p_user_id uuid)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
DECLARE
  user_json jsonb;
BEGIN
  SELECT to_jsonb(up) INTO user_json
  FROM public.user_profiles up
  WHERE up.id = p_user_id
  LIMIT 1;

  IF user_json IS NULL THEN
    -- not found: return an application-level error envelope
    RETURN jsonb_build_object(
      'ok', FALSE,
      'error', 'not_found',
      'message', format('No user found for id: %s', p_user_id)
    );
  END IF;

  RETURN jsonb_build_object(
    'ok', TRUE,
    'data', user_json
  );

EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object(
      'ok', FALSE,
      'error', 'internal_error',
      'message', SQLERRM
    );
END;
$function$
;

CREATE OR REPLACE FUNCTION public.hash_text(txt text)
 RETURNS text
 LANGUAGE sql
AS $function$
  select encode(digest(txt, 'sha256'), 'hex');
$function$
;

CREATE OR REPLACE FUNCTION public.increment_invite_attempts(f_id uuid)
 RETURNS void
 LANGUAGE plpgsql
AS $function$
begin
  update friendships set meta = jsonb_set(coalesce(meta, '{}'::jsonb), '{invite_attempts}',
    to_jsonb(coalesce((meta->>'invite_attempts')::int, 0) + 1), true)
  where id = f_id;
end;
$function$
;

CREATE OR REPLACE FUNCTION public.match_contact_hashes(hashes text[])
 RETURNS TABLE(profile_id uuid, phone_hash text, email_hash text, status text, nickname text, avatar_url text, updated_at timestamp with time zone)
 LANGUAGE sql
 STABLE
AS $function$
SELECT id::uuid AS profile_id,
       phone_hash,
       email_hash,
       status,
       nickname,      -- Changed to nickname
       avatar_url,
       updated_at
FROM user_profiles
WHERE (phone_hash = ANY(hashes) OR email_hash = ANY(hashes));
$function$
;

CREATE OR REPLACE FUNCTION public.rpc_add_member_to_group(p_group_id uuid, p_inviter uuid, p_friend uuid, p_meta jsonb, p_invite_token text, p_invite_sent_at timestamp with time zone, p_invite_expires_at timestamp with time zone, p_invite_channel friendship_source_enum)
 RETURNS jsonb
 LANGUAGE plpgsql
AS $function$
declare
  v_inviter_member record;
  v_friend record;
  v_group_member record;
begin
  -- 1) Verify inviter is a member of the group
  select id, role into v_inviter_member
  from public.group_members
  where group_id = p_group_id
    and user_id = p_inviter
  limit 1;

  if not found then
    -- fail fast; edge function should catch this and return 403
    raise exception 'inviter_not_member';
  end if;

  -- Optional: if you want to restrict to admin/owner only, add:
  -- if v_inviter_member.role not in ('admin','owner') then
  --   raise exception 'inviter_not_authorized';
  -- end if;

  -- 2) friendships: insert only if a row for (requester_id, friend_id) does not exist.
  select * into v_friend
  from public.friendships
  where requester_id = p_inviter
    and friend_id = p_friend
  limit 1;

  if not found then
    insert into public.friendships(
      requester_id,
      friend_id,
      source,
      status,
      meta,
      created_at
    ) values (
      p_inviter,
      p_friend,
      coalesce(p_invite_channel, 'app'),
      'pending',
      coalesce(p_meta, '{}'::jsonb),
      now()
    );
  end if;

  -- 3) group_members: if exists -> UPDATE status='active', invited_by=inviter, removed_by=NULL
  select * into v_group_member
  from public.group_members
  where group_id = p_group_id
    and user_id = p_friend
  limit 1;

  if found then
    update public.group_members
    set status = 'active',
        invited_by = p_inviter,
        removed_by = null
    where id = v_group_member.id;
  else
    insert into public.group_members(
      group_id,
      user_id,
      invited_by,
      role,
      status,
      joined_at
    ) values (
      p_group_id,
      p_friend,
      p_inviter,
      'member',
      'active',
      now()
    );
  end if;

  -- Optionally persist an invites log if you have an 'invites' table.
  -- Example (uncomment and adapt if you have this table):
  -- insert into public.invites (group_id, friend_id, inviter_id, invite_token, invite_sent_at, invite_expires_at, channel, meta, created_at)
  -- values (p_group_id, p_friend, p_inviter, p_invite_token, p_invite_sent_at, p_invite_expires_at, p_invite_channel, p_meta, now());

  return jsonb_build_object(
    'ok', true,
    'friend_profile_id', p_friend,
    'invite_token', p_invite_token,
    'message', 'rpc_add_invite_to_group: success'
  );
exception
  when others then
    -- Normalize the error message so edge function can interpret
    return jsonb_build_object(
      'ok', false,
      'error', sqlstate || ': ' || coalesce(nullif(sqlerrm, ''), 'unknown error')
    );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.rpc_add_member_to_group(p_group_id uuid, p_inviter uuid, p_friend uuid, p_meta jsonb, p_invite_token text, p_invite_sent_at timestamp with time zone, p_invite_expires_at timestamp with time zone, p_invite_channel text)
 RETURNS jsonb
 LANGUAGE plpgsql
AS $function$
declare
  v_inviter_member record;
  v_friend record;
  v_group_member record;
begin
  -- 1) Verify inviter is a member of the group
  select id, role into v_inviter_member
  from public.group_members
  where group_id = p_group_id
    and user_id = p_inviter
  limit 1;

  if not found then
    -- fail fast; edge function should catch this and return 403
    raise exception 'inviter_not_member';
  end if;

  -- Optional: if you want to restrict to admin/owner only, add:
  -- if v_inviter_member.role not in ('admin','owner') then
  --   raise exception 'inviter_not_authorized';
  -- end if;

  -- 2) friendships: insert only if a row for (requester_id, friend_id) does not exist.
  select * into v_friend
  from public.friendships
  where requester_id = p_inviter
    and friend_id = p_friend
  limit 1;

  if not found then
    insert into public.friendships(
      requester_id,
      friend_id,
      source,
      status,
      meta,
      created_at
    ) values (
      p_inviter,
      p_friend,
      coalesce(p_invite_channel, 'app'),
      'pending',
      coalesce(p_meta, '{}'::jsonb),
      now()
    );
  end if;

  -- 3) group_members: if exists -> UPDATE status='active', invited_by=inviter, removed_by=NULL
  select * into v_group_member
  from public.group_members
  where group_id = p_group_id
    and user_id = p_friend
  limit 1;

  if found then
    update public.group_members
    set status = 'active',
        invited_by = p_inviter,
        removed_by = null
    where id = v_group_member.id;
  else
    insert into public.group_members(
      group_id,
      user_id,
      invited_by,
      role,
      status,
      joined_at
    ) values (
      p_group_id,
      p_friend,
      p_inviter,
      'member',
      'active',
      now()
    );
  end if;

  -- Optionally persist an invites log if you have an 'invites' table.
  -- Example (uncomment and adapt if you have this table):
  -- insert into public.invites (group_id, friend_id, inviter_id, invite_token, invite_sent_at, invite_expires_at, channel, meta, created_at)
  -- values (p_group_id, p_friend, p_inviter, p_invite_token, p_invite_sent_at, p_invite_expires_at, p_invite_channel, p_meta, now());

  return jsonb_build_object(
    'ok', true,
    'friend_profile_id', p_friend,
    'invite_token', p_invite_token,
    'message', 'rpc_add_invite_to_group: success'
  );
exception
  when others then
    -- Normalize the error message so edge function can interpret
    return jsonb_build_object(
      'ok', false,
      'error', sqlstate || ': ' || coalesce(nullif(sqlerrm, ''), 'unknown error')
    );
end;
$function$
;

CREATE OR REPLACE FUNCTION public.update_user_profile(p_user_id uuid, p_nickname text DEFAULT NULL::text, p_theme theme_enum DEFAULT NULL::theme_enum)
 RETURNS jsonb
 LANGUAGE plpgsql
 SECURITY DEFINER
AS $function$
DECLARE
  updated_row public.user_profiles%ROWTYPE;
BEGIN
  -- If caller didn't provide any updatable fields, return current row (no-op)
  IF p_nickname IS NULL AND p_theme IS NULL THEN
    SELECT up.*
    INTO updated_row
    FROM public.user_profiles up
    WHERE up.id = p_user_id
    LIMIT 1;

    IF updated_row IS NULL THEN
      RETURN jsonb_build_object(
        'ok', FALSE,
        'error', 'not_found',
        'message', format('No user found for id: %s', p_user_id)
      );
    END IF;

    RETURN jsonb_build_object(
      'ok', TRUE,
      'data', to_jsonb(updated_row),
      'message', 'no_changes'  -- indicates nothing was changed
    );
  END IF;

  -- Perform update: only change fields that are explicitly provided (IS NOT NULL).
  UPDATE public.user_profiles up
  SET
    nickname   = CASE WHEN p_nickname IS NOT NULL THEN p_nickname ELSE up.nickname END,
    theme      = CASE WHEN p_theme    IS NOT NULL THEN p_theme    ELSE up.theme    END,
    updated_at = now()
  WHERE up.id = p_user_id
  RETURNING up.* -- return entire row
  INTO updated_row;

  IF updated_row IS NULL THEN
    RETURN jsonb_build_object(
      'ok', FALSE,
      'error', 'not_found',
      'message', format('No user found for id: %s', p_user_id)
    );
  END IF;

  RETURN jsonb_build_object(
    'ok', TRUE,
    'data', to_jsonb(updated_row)
  );

EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object(
      'ok', FALSE,
      'error', 'internal_error',
      'message', SQLERRM
    );
END;
$function$
;

CREATE OR REPLACE FUNCTION public.upsert_user_profile(_id uuid, _email text, _phone text, _avatar_url text, _metadata jsonb)
 RETURNS void
 LANGUAGE plpgsql
AS $function$
begin
  insert into user_profiles(id, email, phone, avatar_url, metadata, phone_hash, email_hash)
  values (_id, _email, _phone, _avatar_url, _metadata,
          case when _phone is not null then encode(digest(lower(_phone)::text, 'sha256'), 'hex') end,
          case when _email is not null then encode(digest(lower(_email)::text, 'sha256'), 'hex') end)
  on conflict (id) do update
  set email = coalesce(excluded.email, user_profiles.email),
      phone = coalesce(excluded.phone, user_profiles.phone),
      avatar_url = coalesce(excluded.avatar_url, user_profiles.avatar_url),
      metadata = user_profiles.metadata || excluded.metadata,
      phone_hash = coalesce(excluded.phone_hash, user_profiles.phone_hash),
      email_hash = coalesce(excluded.email_hash, user_profiles.email_hash),
      updated_at = now();
end;
$function$
;

CREATE OR REPLACE FUNCTION public.user_profiles_before_insert_update()
 RETURNS trigger
 LANGUAGE plpgsql
AS $function$
declare
  pepper text := null;
begin
  -- normalization examples
  if new.email is not null then
    new.email := lower(trim(new.email));
  end if;

  if new.phone is not null then
    new.phone := regexp_replace(trim(new.phone), '\s+', '', 'g'); -- simple strip spaces; prefer E.164 normalization in app
  end if;

  -- update timestamps
  new.updated_at := now();
  if tg_op = 'INSERT' then
    new.created_at := coalesce(new.created_at, now());
  end if;

  -- OPTIONAL: compute server-side hash using pgcrypto digest (SHA256)
  -- WARNING: using server-side digest alone is vulnerable to precomputation; prefer HMAC with a server secret.
  -- Uncomment if you want server-side SHA256 (not HMAC):
  -- if new.phone is not null then
  --   new.phone_hash := encode(digest(new.phone, 'sha256'), 'hex');
  -- end if;
  -- if new.email is not null then
  --   new.email_hash := encode(digest(new.email, 'sha256'), 'hex');
  -- end if;

  return new;
end; $function$
;



